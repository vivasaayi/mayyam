FROM python:3.9-slim

# Install required packages
RUN pip install --no-cache-dir kafka-python

# Copy the test data generator script
COPY generate_test_kafka_data.py /app/generate_test_kafka_data.py

# Set working directory
WORKDIR /app

# Default command (can be overridden)
CMD ["python", "generate_test_kafka_data.py"]